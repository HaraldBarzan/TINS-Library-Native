<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <GenerateDependencyFile>false</GenerateDependencyFile>
    <NoWarn>$(NoWarn);NU5128;NU5100</NoWarn>
    <PackageId>TINS.Native.win-x64</PackageId>
    <Description>Native runtime assets (OpenBLAS) for TINS.Core on win-x64, BSD-3-Clause throughout. For FFTW (GPL-2.0-or-later), add TINS.Native.FFTW.win-x64 separately -- see that package's description for why it's a separate opt-in reference instead of bundled here.</Description>
    <PackageTags>Neuroscience, Native, OpenBLAS, win-x64</PackageTags>
    <NativeStagingDir Condition="'$(NativeStagingDir)'==''">$(MSBuildThisFileDirectory)..\..\artifacts\runtimes\win-x64\native</NativeStagingDir>
  </PropertyGroup>

  <ItemGroup>
    <None Include="$(NativeStagingDir)\*" Pack="true"
          PackagePath="runtimes/win-x64/native/%(Filename)%(Extension)" Visible="false" />
  </ItemGroup>

</Project>
